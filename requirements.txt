# Runtime — lower bounds keep this installable on Python 3.11 (Docker) and
# newer interpreters alike. Pin exact versions in a lockfile for production.
fastapi>=0.111
uvicorn[standard]>=0.30
pandas>=2.2
python-multipart>=0.0.9
pydantic>=2.7

# Dev / test
pytest>=8.2
httpx>=0.27
